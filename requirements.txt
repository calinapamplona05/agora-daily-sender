# No third-party packages needed — send_report.py uses only Python standard library.
# (smtplib, email, glob, os, logging, datetime are all built-in)
# This file is kept so the GitHub Actions workflow step doesn't fail.
